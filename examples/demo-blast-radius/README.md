# KBeacon blast-radius demo

This demo creates a small multi-namespace application graph that shows why KBeacon exists.

It creates:

- Secret/payments-db in namespace payments.
- Secret/stripe-api in namespace payments.
- Secret/platform-ca in namespace shared.
- Deployment/payments-api.
- Deployment/checkout-worker.
- Deployment/reports-api.
- One intentionally unresolved dependency: Secret/legacy-payment-token.

Expected blast-radius story:

| Secret | Expected impact |
| --- | --- |
| payments/payments-db | Referenced by payments-api, checkout-worker, and reports-api. |
| payments/stripe-api | Referenced by checkout-worker. |
| shared/platform-ca | Referenced explicitly by payments-api and reports-api. |
| payments/legacy-payment-token | Referenced explicitly but intentionally missing, so it should appear as unresolved. |

## Apply demo resources

    ./examples/demo-blast-radius/run.sh apply

## Inspect resources

    ./examples/demo-blast-radius/run.sh status

## Query KBeacon

Install or upgrade KBeacon first, then port-forward the Agent API.

    kubectl -n kbeacon-system port-forward svc/kbeacon 8081:8080

In another terminal:

    curl -sS http://127.0.0.1:8081/api/v1/secrets/payments/payments-db/impact | jq ".data.summary"
    curl -sS http://127.0.0.1:8081/api/v1/secrets/payments/legacy-payment-token/impact | jq ".data.secret"
    curl -sS http://127.0.0.1:8081/api/v1/workloads/payments/Deployment/payments-api/dependencies | jq ".data.dependencies"

<!-- kbeacon-demo-verified-output:start -->
## Verified demo output

The demo was validated against a live KBeacon Agent through the local port-forwarded API.

Timestamps and Kubernetes UIDs are intentionally not documented because they are generated by the cluster. Global dependency-map totals can also vary if the cluster contains workloads outside this demo.

| Query | Expected result |
| --- | --- |
| `payments/payments-db` impact | `exists=true`, `affectedWorkloadCount=3`, `affectedTeamCount=3`, `affectedNamespaceCount=2`, `unresolvedReferenceCount=0`, `impactScore=46` |
| `payments/payments-db` discovery modes | `{"explicit": 1, "infer": 2}` |
| `payments/legacy-payment-token` impact | `exists=false`, `affectedWorkloadCount=1`, `unresolvedReferenceCount=1`, `impactScore=38` |
| `payments/legacy-payment-token` discovery modes | `{"explicit": 1}` |
| `Deployment/payments-api` dependencies | `dependencyCount=3`, `unresolvedCount=1` |
| Global dependency map sample | `nodes=30`, `edges=10` |

Expected `Deployment/payments-api` dependencies:

| Secret | Resolved | Discovery mode |
| --- | --- | --- |
| `payments/legacy-payment-token` | `false` | `explicit` |
| `payments/payments-db` | `true` | `infer` |
| `shared/platform-ca` | `true` | `explicit` |

The important behavior is that `payments/payments-db` is resolved and high-impact, while `payments/legacy-payment-token` is intentionally unresolved but still visible in the graph.
<!-- kbeacon-demo-verified-output:end -->

## Delete demo resources

    ./examples/demo-blast-radius/run.sh delete
